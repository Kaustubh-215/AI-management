services:

  mysql:
    image: mysql:8.4

    container_name: ai-mysql

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: RootPassword123!
      MYSQL_DATABASE: ai_image_db
      MYSQL_USER: ai_image_user
      MYSQL_PASSWORD: YourStrongPassword123!

    volumes:
      - mysql_data:/var/lib/mysql

    expose:
      - "3306"

  backend:
    image: kaustubhsnehu/ai-image-backend:latest

    container_name: ai-backend

    env_file:
      - .env

    expose:
      - "8000"

    depends_on:
      - mysql

    restart: always

  nginx:
    image: nginx:latest

    container_name: ai-nginx

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/html:/usr/share/nginx/html:ro

    depends_on:
      - backend

    restart: always

  prometheus:
    image: prom/prometheus:latest

    container_name: ai-prometheus

    expose:
      - "9090"

    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.route-prefix=/prometheus"
      - "--web.external-url=https://54.68.149.130.nip.io/prometheus"

    volumes:
      - ../monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml

    restart: always

  grafana:
    image: grafana/grafana:latest

    container_name: ai-grafana

    expose:
      - "3000"

    environment:
      GF_SERVER_ROOT_URL: https://54.68.149.130.nip.io/grafana
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"

    volumes:
      - grafana_data:/var/lib/grafana
      - ../monitoring/grafana/provisioning:/etc/grafana/provisioning

    restart: always

  loki:
    image: grafana/loki:latest

    container_name: ai-loki

    expose:
      - "3100"

    command:
      - "-config.file=/etc/loki/loki-config.yml"

    volumes:
      - ../monitoring/loki/loki-config.yml:/etc/loki/loki-config.yml

    restart: always

  promtail:
    image: grafana/promtail:latest

    container_name: ai-promtail

    command:
      - "-config.file=/etc/promtail/promtail-config.yml"

    volumes:
      - ../monitoring/promtail/promtail-config.yml:/etc/promtail/promtail-config.yml
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock

    depends_on:
      - loki

    restart: always

  tempo:
    image: grafana/tempo:latest

    container_name: ai-tempo

    expose:
      - "3200"
      - "4317"
      - "4318"

    command:
      - "-config.file=/etc/tempo/tempo.yml"

    volumes:
      - ../monitoring/tempo/tempo.yml:/etc/tempo/tempo.yml

    restart: always

  node-exporter:
    image: prom/node-exporter:latest

    container_name: ai-node-exporter

    ports:
      - "9100:9100"

    restart: always

volumes:
  mysql_data:
  grafana_data:
